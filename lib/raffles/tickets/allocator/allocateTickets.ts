import {
  AllocateTicketsInput,
  AllocateTicketsResult
} from "./types"

export async function allocateTickets(
  input: AllocateTicketsInput
): Promise<AllocateTicketsResult> {

  throw new Error(
    "allocateTickets_not_implemented"
  )

}